---
name: Bug report
about: Something behaved differently than documented
title: ""
labels: bug
---

**What happened**

**What you expected**

**Evidence**

The relevant lines from `<vault>/.a5n-logs/` (the daily log, or the
lint/digest log). For ingest problems, the `capture summary:` line and the
unit's log block are usually enough.

**Environment**

- OS:
- Python version (`python3 --version`):
- Runner engine and model (`python3 scripts/config.py --check` output, minus
  any private paths):

**Transcript format involved** (if any): Claude Code / Codex / other
